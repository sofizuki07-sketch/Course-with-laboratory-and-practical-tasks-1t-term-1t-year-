# Справочный материал: Документирование кода на языке C (в стиле Google)

##  Зачем документировать код?

**Хорошая документация:**
- Объясняет **ЗАЧЕМ**, а не как работает код
- Помогает другим (и вам через месяц) понять логику
- Облегчает отладку и поддержку
- Является частью профессиональной культуры программирования

##  Основные принципы

### 1. Комментарии vs Документация
- **Комментарии** — объясняют сложные места в реализации
- **Документация** — описывает что делает функция/модуль и как им пользоваться

### 2. Правило "Золотой середины"
- Не документируйте очевидное: `x = x + 1; // увеличиваем x на 1`
- Но обязательно документируйте неочевидное: почему выбрали именно этот алгоритм

## 📝 Типы комментариев в C

### 1. Однострочные комментарии (для пояснений в коде)
```c
// Проверяем, что указатель не нулевой перед разыменованием
if (ptr != NULL) {
    *ptr = value;
}

// Используем быстрый алгоритм для маленьких массивов
// (подробности см. в документации к функции)
```

### 2. Многострочные комментарии (для функций и сложных блоков)
```c
/*
 * Сортировка массива методом пузырька.
 * 
 * Особенности:
 * - Работает только с массивами до 1000 элементов
 * - Для больших массивов используйте qsort()
 * 
 * Время работы: O(n^2) в худшем случае
 */
void bubble_sort(int arr[], int size) {
    // ...
}
```

##  Документирование функций (Doxygen-стиль)

### Базовый шаблон
```c
/**
 * @brief Краткое описание функции (одна строка)
 *
 * Подробное описание функции. Может занимать несколько абзацев.
 * Описывает что делает функция, какие алгоритмы использует,
 * ограничения и особенности.
 *
 * @param param1 Описание первого параметра
 * @param param2 Описание второго параметра
 * @param[out] result Параметр, в который записывается результат
 *
 * @return Что возвращает функция
 * @retval 0 в случае успеха
 * @retval -1 в случае ошибки
 *
 * @note Важное замечание для пользователя
 * @warning Предупреждение об опасностях
 * @see related_function() - ссылка на связанную функцию
 *
 * @example
 *   int arr[] = {3, 1, 2};
 *   bubble_sort(arr, 3);  // результат: {1, 2, 3}
 */
int function_name(type param1, type param2, type* result);
```

### Примеры хорошей документации

#### Пример 1: Функция вычисления факториала
```c
/**
 * @brief Вычисляет факториал числа
 *
 * Вычисляет n! = 1 × 2 × ... × n.
 * Использует итеративный алгоритм для избежания переполнения стека.
 *
 * @param n Неотрицательное целое число
 *
 * @return Факториал числа n
 * @retval 1 если n = 0 или n = 1
 * @retval -1 если n < 0 (ошибка)
 * @retval -2 если произошло переполнение
 *
 * @warning Не используйте для n > 20 из-за переполнения 64-битного целого
 * @note Для больших чисел используйте библиотеку big integers
 *
 * @example
 *   long long result = factorial(5);  // result = 120
 */
long long factorial(int n) {
    if (n < 0) return -1;           // Ошибка: отрицательное число
    if (n > 20) return -2;          // Ошибка: возможно переполнение
    
    long long result = 1;
    for (int i = 2; i <= n; i++) {
        // Проверка переполнения перед умножением
        if (result > LLONG_MAX / i) {
            return -2;               // Переполнение
        }
        result *= i;
    }
    return result;
}
```

#### Пример 2: Функция работы со структурой
```c
/**
 * @brief Представляет студента в системе
 */
typedef struct {
    char name[50];      ///< Имя студента (максимум 49 символов + '\0')
    int age;            ///< Возраст в годах (должен быть от 16 до 100)
    float gpa;          ///< Средний балл (от 0.0 до 5.0)
} Student;

/**
 * @brief Создает нового студента с валидацией данных
 *
 * Проверяет корректность входных данных:
 * - Имя не пустое и не превышает допустимую длину
 * - Возраст в допустимом диапазоне
 * - GPA в допустимом диапазоне
 *
 * @param[in] name Имя студента
 * @param[in] age Возраст студента
 * @param[in] gpa Средний балл
 * @param[out] student Указатель на структуру Student для заполнения
 *
 * @return Код результата операции
 * @retval 0 Успех
 * @retval -1 Некорректное имя (NULL, пустое или слишком длинное)
 * @retval -2 Некорректный возраст
 * @retval -3 Некорректный GPA
 * @retval -4 Нулевой указатель на student
 *
 * @note Выделяет память для имени внутри структуры
 * @warning Не освобождает память при ошибке - вызывающая сторона должна
 *          обработать ошибку и при необходимости освободить ресурсы
 */
int create_student(const char* name, int age, float gpa, Student* student) {
    // Проверка входных параметров
    if (student == NULL) return -4;
    if (name == NULL || strlen(name) == 0) return -1;
    if (strlen(name) >= 50) return -1;
    if (age < 16 || age > 100) return -2;
    if (gpa < 0.0 || gpa > 5.0) return -3;
    
    // Копирование данных
    strncpy(student->name, name, 49);
    student->name[49] = '\0';
    student->age = age;
    student->gpa = gpa;
    
    return 0;
}
```

#### Пример 3: Функция с обработкой ошибок
```c
/**
 * @brief Читает матрицу из файла
 *
 * Формат файла:
 * - Первая строка: rows cols
 * - Далее rows строк по cols чисел через пробел
 *
 * @param[in] filename Имя файла для чтения
 * @param[out] rows Указатель для сохранения количества строк
 * @param[out] cols Указатель для сохранения количества столбцов
 * @param[out] matrix Указатель на указатель для сохранения матрицы
 *
 * @return Код ошибки
 * @retval 0 Успешное чтение
 * @retval -1 Не удалось открыть файл
 * @retval -2 Неверный формат файла
 * @retval -3 Ошибка выделения памяти
 * @retval -4 Некорректные размеры матрицы
 *
 * @note Выделяет память под матрицу. Вызывающая сторона должна
 *       освободить память с помощью free_matrix()
 * @see free_matrix() - для освобождения памяти
 *
 * @example
 *   int rows, cols;
 *   int** matrix;
 *   int result = read_matrix("data.txt", &rows, &cols, &matrix);
 *   if (result == 0) {
 *       // Использовать матрицу
 *       free_matrix(matrix, rows);
 *   }
 */
int read_matrix(const char* filename, int* rows, int* cols, int*** matrix) {
    // ... реализация ...
}
```

##  Документирование модулей и файлов

### В начале файла .h (заголовочный файл)
```c
/**
 * @file student_manager.h
 * @brief Модуль для управления студентами
 *
 * Этот модуль предоставляет функции для создания, изменения
 * и удаления записей о студентах. Все функции потокобезопасны.
 *
 * @author Иван Иванов
 * @date 2024
 * @version 1.0
 * @copyright MIT License
 *
 * @defgroup student_management Управление студентами
 * @{
 */

#ifndef STUDENT_MANAGER_H
#define STUDENT_MANAGER_H

// ... содержимое файла ...

#endif /* STUDENT_MANAGER_H */

/** @} */ // конец группы student_management
```

### В начале файла .c (реализация)
```c
/**
 * @file student_manager.c
 * @brief Реализация модуля управления студентами
 *
 * @details
 * Этот файл содержит реализацию функций, объявленных в student_manager.h.
 * Используется динамическое выделение памяти для хранения студентов.
 *
 * Внутренняя структура данных:
 * - Массив указателей на структуры Student
 * - Динамическое увеличение размера при необходимости
 *
 * @note Для работы требуется стандартная библиотека C и stdlib.h
 */

#include "student_manager.h"
#include <stdlib.h>
#include <string.h>

// ... реализация функций ...
```

##  Правила для обучающихся

### Обязательно документируйте:
1. **Каждую функцию** — что делает, параметры, возвращаемое значение
2. **Сложные алгоритмы** — почему выбран этот алгоритм
3. **Неочевидные решения** — почему сделано именно так
4. **Ограничения** — что функция НЕ делает, граничные условия
5. **Примеры использования** — как правильно вызывать функцию

### Избегайте:
```c
// ПЛОХО: Бесполезный комментарий
x++;  // увеличиваем x на 1

// ПЛОХО: Комментарий, который не синхронизирован с кодом
x = 5;  // устанавливаем x равным 6

// ХОРОШО: Объяснение причины
// Используем битовый сдвиг вместо умножения для оптимизации скорости
x = y << 2;  // эквивалентно y * 4
```

### Шаблон для лабораторных работ:
```c
/**
 * Лабораторная работа №3, Задача 2
 * Студент: Иванов И.И.
 * Группа: ПИ-202
 * Дата: 2024-10-15
 *
 * Задача: Реализовать функцию поиска максимального элемента в массиве
 */

#include <stdio.h>

/**
 * @brief Находит максимальный элемент в массиве
 *
 * Проходит по всем элементам массива и находит максимальное значение.
 * Используется линейный поиск O(n).
 *
 * @param arr Указатель на массив целых чисел
 * @param size Размер массива
 *
 * @return Максимальный элемент массива
 * @retval INT_MIN если size <= 0 или arr == NULL
 *
 * @note Для пустого массива возвращает INT_MIN
 * @warning Не проверяет переполнение при работе с большими числами
 */
int find_max(const int arr[], int size) {
    if (arr == NULL || size <= 0) {
        return INT_MIN;
    }
    
    int max = arr[0];
    for (int i = 1; i < size; i++) {
        if (arr[i] > max) {
            max = arr[i];
        }
    }
    return max;
}

/**
 * @brief Тестирует функцию find_max()
 *
 * Создает тестовые массивы и проверяет корректность работы find_max().
 * Выводит результаты тестов в консоль.
 *
 * @return Всегда возвращает 0
 */
int test_find_max(void) {
    printf("=== Тестирование find_max() ===\n");
    
    // Тест 1: Нормальный массив
    int arr1[] = {3, 1, 4, 1, 5, 9};
    int max1 = find_max(arr1, 6);
    printf("Тест 1: %s (ожидалось 9, получено %d)\n", 
           max1 == 9 ? "ПРОЙДЕН" : "НЕ ПРОЙДЕН", max1);
    
    // ... больше тестов ...
    
    return 0;
}

int main(void) {
    test_find_max();
    return 0;
}
```


**Итог:** Документация — это не формальность, а способ сделать ваш код понятным, надежным и профессиональным. Хорошо задокументированный код экономит время и предотвращает ошибки!
